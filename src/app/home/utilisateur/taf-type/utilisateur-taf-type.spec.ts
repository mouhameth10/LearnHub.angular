import { UtilisateurTafType } from './utilisateur-taf-type';

describe('UtilisateurTafType', () => {
  it('should create an instance', () => {
    expect(new UtilisateurTafType()).toBeTruthy();
  });
});
